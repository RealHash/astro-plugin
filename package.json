{
  "name": "astro-symbols-docs-addon",
  "version": "0.1.0",
  "private": true,
  "description": "Google Docs editor add-on to insert astrological symbols from a visual sidebar.",
  "scripts": {
    "catalog:sync": "node scripts/sync-catalog.mjs",
    "login": "clasp login",
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "clasp deploy",
    "version": "clasp version",
    "logs": "clasp logs --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0"
  }
}
